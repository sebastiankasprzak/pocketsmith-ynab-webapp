{
  "name": "infrastructure",
  "version": "0.1.0",
  "bin": {
    "infrastructure": "bin/infrastructure.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "cdk:deploy": "cdk deploy",
    "lambda:install": "cd lambda/shared && npm install && cd ../accounts && npm install && cd ../balances && npm install && cd ../sync-monitoring && npm install && cd ../parameter-store && npm install",
    "lambda:build": "cd lambda/shared && npm run build && cd ../accounts && npm run build && cd ../balances && npm run build && cd ../sync-monitoring && npm run build && cd ../parameter-store && npm run build",
    "lambda:test": "cd lambda/accounts && npm test && cd ../balances && npm test && cd ../sync-monitoring && npm test",
    "lambda:clean": "rm -rf lambda/*/dist lambda/*/node_modules"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "20.12.7",
    "aws-cdk": "^2.1025.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "~5.4.5"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.212.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
